// obj_final_level_controller Create Event

alarm[0] = room_speed * 5; // 5 seconds until first spawn
tentacle_count = 0;        // Tracks how many times tentacles have appeared
max_tentacle = 5;          // After this many, the portal will show